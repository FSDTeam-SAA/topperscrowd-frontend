import LoginForm from "@/features/auth/components/LoginForm";

export const metadata = {
  title: "Login | Thorian",
  description: "Sign in to your Thorian account",
};

export default function LoginPage() {
  return <LoginForm />;
}
