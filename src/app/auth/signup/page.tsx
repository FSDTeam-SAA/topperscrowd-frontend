import SignupForm from "@/features/auth/components/SignupForm";

export const metadata = {
  title: "Sign Up | Thorian",
  description: "Create a new Thorian account",
};

export default function SignupPage() {
  return <SignupForm />;
}
